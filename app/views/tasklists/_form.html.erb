<%= form_with(model: tasklist) do |form| %>
  <% if tasklist.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tasklist.errors.count, "error") %> prohibited this tasklist from being saved:</h2>

      <ul>
        <% tasklist.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block", class: "form-label" %>
    <%= form.text_field :title, class: "form-control mb-3" %>
  </div>

  <div>
    <%= form.label :description, style: "display: block", class: "form-label" %>
    <%= form.text_area :description, class: "form-control mb-3" %>
  </div>

  <div>
    <%= form.label :deadline, style: "display: block", class: "form-label" %>
    <%= form.date_field :deadline, class: "form-control mb-3" %>
  </div>
  <div>
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>

